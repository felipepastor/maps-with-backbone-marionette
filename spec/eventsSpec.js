require(["controllers/MainCtrl"], function (MainController) {
    /*describe("events tests", function () {
        //var service, widget, collection, storage,
        //    irrelevantLink = {description: 'desc1', url: 'url1'};

        beforeEach(function () {
            console.log('teste');
            //storage = {
            //    save: function () {
            //    }
            //};
            //widget = new Views.AddLink();
            //collection = new Models.LinkCollection();
            //service = Actions.createLinkService(widget, collection, storage);
        });

        it("adds the link to the collection", function () {
            console.log('primeiro teste');
            //fireTheEventWith(irrelevantLink);
            //
            //var link = collection.models[0];
            //expect(link.get('description')).toEqual('desc1');
            //expect(link.get('url')).toEqual('url1');
        });

        it("saves the link in the storage", function () {
            console.log('segundo teste');
            //spyOn(storage, 'save');
            //fireTheEventWith(irrelevantLink);
            //
            //expect(storage.save).toHaveBeenCalled();
        });

        //function fireTheEventWith(link) {
        //    widget.trigger("addLink", link);
        //}
    });*/
});

